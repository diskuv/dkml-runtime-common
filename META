# An empty OCaml package. All the interesting things are not OCaml (ie. scripts)
# in the lib folder. META is needed so that Dune (etc.) can lookup the lib folder
# through findlib.
version = "1.1.0~prerel20"
description = ""
requires = ""
